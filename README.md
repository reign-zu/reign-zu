```javascript
const Reigns = {
"canCode": ["C", "C++", "Bash", "Java", "Python", "JavaScript", "NodeJS", "SQL, "HTML", "XML", "CSS"];
}

```

```javascript
if(atHome){
console.log("There's no place like 127.0.0.1");
} else{
console.log("Working on the codes.");
}
```
